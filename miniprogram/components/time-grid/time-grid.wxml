<view class="time-grid">
  <view class="grid-header">
    <text class="hint">点击选择时段（可连选）</text>
    <view class="legend">
      <view class="legend-item"><view class="dot available"></view><text>可选</text></view>
      <view class="legend-item"><view class="dot occupied"></view><text>已占</text></view>
      <view class="legend-item"><view class="dot selected"></view><text>已选</text></view>
    </view>
  </view>
  <view class="grid-body">
    <view
      wx:for="{{slots}}"
      wx:key="time"
      class="slot {{item.status}}"
      data-index="{{index}}"
      bindtap="onSlotTap"
    >
      <text>{{item.time}}</text>
    </view>
  </view>
  <view wx:if="{{selectedStart}}" class="selection-info">
    <text>已选：{{selectedStart}} - {{selectedEnd || '请选择结束时段'}}</text>
  </view>
</view>
