<view class="empty-state">
  <text class="empty-icon">{{icon}}</text>
  <text class="empty-text">{{text}}</text>
  <view wx:if="{{btnText}}" class="empty-btn" bindtap="onBtnTap">
    <text>{{btnText}}</text>
  </view>
</view>
